@model Dominio.Pedido

@{
    ViewData["Title"] = "Tu pedido";
}

@if (Model == null)
{
    <h1>Tu pedido</h1>
    <section class="container border">
        <div class="row">
            <div class="col-12 d-flex flex-column justify-content-center text-center p-3">
                <figure>
                    <img class="empty-order" src="~/assets/img/20.svg" alt="Plato vacío" />
                </figure>
                <h2 class="lead">Tu orden todavía está vacía</h2>
            </div>
            
        </div>
    </section>
}
else
{
    <h1>View</h1>

    <div>
        <h4>Pedido</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Date)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Date)
            </dd>
        </dl>
    </div>
    <div>
        @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
        <a asp-action="Index">Back to List</a>
    </div>

}